// Complete Class card by extending abstract Class PlayingCard and implementing the Comparable interface
import java.util.Random;
public class Card extends PlayingCard implements Comparable{
  public Card(){
    /*suit=new Random.nextInt(4);
    rank=new Random.nextInt(cards.size());*/
  }
}
/*
 * constructors:
 *   card(int suit, int rank)
 * accessors:
 *   toString()
 *   compareTo(...):comparable by rank
  
